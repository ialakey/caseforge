name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The version comes from "packageManager" in package.json; passing it here
      # as well makes the action bail out on a version mismatch.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile
        env:
          # prisma generate runs on postinstall and only needs a parseable URL
          DATABASE_URL: postgresql://user:pass@localhost:5432/db?schema=public

      - name: Build shared
        run: pnpm --filter @caseforge/shared build

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test
